# NY Sales Tax Calculator 🗽

RESTful API сервіс для автоматичного розрахунку податку з продажу для штату Нью-Йорк на основі географічних координат замовлення.

## 💡 Бізнес-проблема та архітектурне рішення

За умовами системи, мобільний застосунок передає для доставки виключно точні GPS-координати (широту та довготу). Складність нарахування податку у штаті Нью-Йорк полягає в тому, що податкові ставки залежать від конкретних географічних юрисдикцій (округів та міст), межі яких є складними багатокутниками (полігонами). 

**Чому класичні підходи не працюють:**
1. Пошук за назвою міста чи поштовим індексом (ZIP-кодом) неможливий через відсутність цих даних у вхідному запиті.
2. Розрахунок перетину координат із полігонами на рівні бекенду при великій кількості замовлень створює критичне навантаження на оперативну пам'ять та процесор.
3. Використання зовнішніх платних сервісів геокодування (Google Maps API тощо) додає мережеві затримки та робить систему залежною.

**Обране рішення: Геопросторова база даних (Spatial Database)**

В основі проєкту лежить перенесення обчислювального навантаження на рівень бази даних за допомогою розширення **PostGIS** для PostgreSQL. 

Механіка роботи системи побудована на таких етапах:
* **Автоматизований Seeding:** При першому запуску система автономно парсить відкриті геодані (`GeoJSON`) з кордонами 62 округів штату Нью-Йорк та зшиває їх із локальним `.csv` файлом податкових ставок.
* **Просторові типи даних:** Юрисдикції зберігаються в БД як об'єкти типу `geometry`, що дозволяє базі створити спеціальні просторові індекси (GIST) для пошуку.
* **Миттєве визначення (ST_Contains):** Координати кожного нового замовлення на льоту конвертуються в геометричну точку. Замість перебору даних EF Core генерує просторовий запит `ST_Contains`, і база даних за мілісекунди повертає округ, у межах якого знаходиться точка доставки.
* **Clean Architecture:** Проєкт розділений на незалежні шари (Domain, Application, Infrastructure, API). Це гарантує, що складна інфраструктурна логіка гео-пошуку повністю ізольована, а контролери та бізнес-логіка залишаються чистими.
* **Контейнеризація:** Застосунок та база даних (з уже встановленим PostGIS) повністю упаковані в Docker.

---

## 🛠 Технологічний стек

* **Бекенд:** C# 14, .NET 10, ASP.NET Core Web API
* **База даних:** PostgreSQL 15 + PostGIS 3.4
* **ORM:** Entity Framework Core 10 (з інтеграцією NetTopologySuite)
* **Парсинг:** CsvHelper, NetTopologySuite.IO.GeoJSON
* **Розгортання:** Docker, Docker Compose
* **Frontend:** React 18, TypeScript, Material UI (MUI), Axios, Vite

---

## 🚀 Як запустити проєкт локально

Для запуску системи вам знадобиться лише встановлений Docker та Docker Desktop (або Docker Engine).

### 1. Запуск через Docker Compose (У три кроки)

Через особливості ініціалізації гео-розширення PostGIS, запуск контейнерів має відбуватися послідовно. Відкрийте термінал у кореневій папці проєкту:

**Крок 1: Запуск бази даних**
Підніміть контейнер із БД командою:
```bash
docker-compose up db -d
```
> ⚠️ **Важливо:** Зачекайте 10-15 секунд, щоб PostgreSQL повністю ініціалізувався та встановив розширення PostGIS.

**Крок 2: Запуск API**
Тепер зберіть та запустіть бекенд:
```bash
docker-compose up api --build -d
```
**Крок 3: Запуск інтерфейсу (Фронтенд)**
Після того, як бекенд став доступним, запустіть клієнтську частину:
```bash
docker-compose up frontend -d
```

### 2. Застосування міграцій


Застосунок налаштований на **автоматичне застосування міграцій** при старті API. Система самостійно створить базу даних, увімкне необхідні розширення PostGIS та створить і заповнить таблиці геоданими.

### 3. Перевірка працездатності

Після успішного запуску перейдіть у браузері за адресою: 
👉 `http://localhost:8080/api/orders`

---
## Доступ до сервісів

Після успішного старту система доступна за адресами:

| Сервіс | URL |
|--------|------|
| 🌐 Frontend UI | http://localhost:5173 |
| 📡 API (Backend) | http://localhost:8080/api/orders |
| 🗄 PostgreSQL | localhost:5433 |

### Параметри БД:
- User: `postgres`
- Password: `12345`
- Database: `survey_db`

---

# 🖥 Frontend — Адмін-панель

Інтерфейс побудований на React + MUI та дозволяє керувати замовленнями через зручний UI.
## 📡 API Ендпоінти

### 1. Імпорт замовлень із CSV файлу
Завантажує файл, розпізнає координати, просторовим пошуком розраховує податки та зберігає масив замовлень у БД.
* **URL:** `POST /api/orders/import`
* **Body:** `multipart/form-data`
* **Ключ:** `file` (тип File, прикріпіть ваш `.csv` файл)

### 2. Створення одиничного замовлення (JSON)
Зручно для швидкого тестування розрахунку без файлів.
* **URL:** `POST /api/orders`
* **Headers:** `Content-Type: application/json`
* **Body:**
```json
{
  "latitude": 40.748817,
  "longitude": -73.985428,
  "subtotal": 100.00,
  "timestamp": "2025-11-04T10:00:00Z"
}
```

### 3. Отримання списку замовлень (з пагінацією та фільтрами)
Повертає збережені замовлення із деталізованими розрахованими сумами податків. Підтримує пагінацію та динамічну фільтрацію.
* **URL:** `GET /api/orders`

**Доступні Query-параметри (Фільтри):**
* `page` (int) — Номер сторінки (за замовчуванням: 1).
* `pageSize` (int) — Кількість записів на сторінку (за замовчуванням: 10).
* `startDate` (DateTime) — Фільтр за датою створення (від). Формат: ISO 8601 (напр., `2025-01-01T00:00:00Z`).
* `endDate` (DateTime) — Фільтр за датою створення (до).
* `minTotalAmount` (decimal) — Мінімальна сума замовлення з урахуванням податків.
* `maxTotalAmount` (decimal) — Максимальна сума замовлення з урахуванням податків.

**Приклад запиту з фільтрами:**
`GET /api/orders?page=1&pageSize=10&minTotalAmount=50&startDate=2025-01-01T00:00:00Z`

**Відповідь:**
```json
{
  "totalCount": 11222,
  "page": 1,
  "pageSize": 10,
  "items": [
    {
      "id": "1001",
      "latitude": 40.748817,
      "longitude": -73.985428,
      "subtotal": 100.00,
      "composite_tax_rate": 0.08875,
      "tax_amount": 8.88,
      "total_amount": 108.88,
      "breakdown": {
        "state_rate": 0.04,
        "county_rate": 0.04875,
        "city_rate": 0.0,
        "special_rates": 0.0
      },
      "jurisdictions": ["New York State", "New York City"]
    }
  ]
}
```

---

## 🧹 Додаткові команди Docker

Якщо вам потрібно повністю очистити базу даних (видалити всі таблиці та дані) і почати з чистого аркуша:
```bash
docker-compose down -v
docker-compose up db -d
# Зачекати 10-15 секунд...
docker-compose up api --build -d
docker-compose up frontend -d
```
